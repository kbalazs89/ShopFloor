<Window x:Class="ShopFloor.CartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopFloor"
        mc:Ignorable="d"
        Title="CartView" Height="568.394" Width="434.4">
    <Grid>
        <ListBox ItemsSource="{Binding PurchasedProducts}" HorizontalAlignment="Left" Height="408" Margin="10,56,0,0" VerticalAlignment="Top" Width="235">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Label Content="{Binding Name}"/>
                        <Label Content="{Binding Price}" FontSize="11" ContentStringFormat="0 M USD"/>
                        <Label Content="{Binding Quantity}" FontSize="11" ContentStringFormat="0 pcs"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Purchase!" HorizontalAlignment="Left" Margin="322,73,0,0" VerticalAlignment="Top" Width="75" Click="PurchaseClick"/>
        <Button Content="Empty Cart" HorizontalAlignment="Left" Margin="322,97,0,0" VerticalAlignment="Top" Width="75" Click="ClearClick"/>
    </Grid>
</Window>
